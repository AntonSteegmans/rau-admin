# RAÚ Standalone Demo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** De RAÚ-app draaien als volledig standalone demo zonder database — een mock Supabase-client op localStorage, gepersonaliseerd voor verzamelaar Maarten, met een asset-/waardelaag en NL-teksten — plus een pitch-spiekbriefje.

**Architecture:** `src/supabase.js` wordt vervangen door een drop-in mock-client met identieke API (`.from().select/insert/update/delete/eq/order/single`, `.auth`, `.storage`). Een store-module (`src/demo/store.js`) houdt alle tabellen in localStorage; een seed-module (`src/demo/seed.js`) levert de genormaliseerde startdata. Componenten blijven ongewijzigd op `main.jsx` na (login overslaan + rol-schakelaar). De grootste complexiteit zit in het parsen van de geneste `select()`-strings; dat wordt met unit-tests afgedekt.

**Tech Stack:** React 18, Vite 6, Three.js, `@supabase/supabase-js` (alleen types/gewoonte — runtime wordt gemockt). Vitest wordt toegevoegd voor de mock-unittests.

---

## File Structure

- `src/demo/seed.js` — **Create.** Genormaliseerde startdata (brands, models, vehicles, clients, profiles, team, services, invoices, messages) gepersonaliseerd voor Maarten. Enige bron van waarheid voor demodata.
- `src/demo/relations.js` — **Create.** Statische FK-relatiemap die de mock gebruikt om geneste selects te resolven.
- `src/demo/select.js` — **Create.** `parseSelect()` + `applySelect()`: parst en past Supabase-achtige select-strings toe. Pure functies, los getest.
- `src/demo/store.js` — **Create.** In-memory tabellen + localStorage-persistentie; `getTable/setTable/resetDemo/genId`.
- `src/demo/mockClient.js` — **Create.** Bouwt de mock-client (`from`, `auth`, `storage`) bovenop store + select.
- `src/supabase.real.js` — **Create.** Kopie van het huidige `src/supabase.js` (echte client), voor latere terugkeer.
- `src/supabase.js` — **Modify.** Exporteert voortaan de mock-client.
- `src/main.jsx` — **Modify.** Login overslaan + rol-schakelaar (Admin ⇄ Maartens portaal).
- `src/demo/__tests__/select.test.js` — **Create.** Unit-tests voor de select-parser.
- `src/demo/__tests__/mockClient.test.js` — **Create.** Unit-tests voor de query-builder tegen de echte select-strings.
- `docs/pitch-maarten.md` — **Create.** Pitch-spiekbriefje.
- `package.json` — **Modify.** Vitest toevoegen + `test`-script.
- `src/RauClient.jsx` / `src/RauAdmin.jsx` — **Modify.** Asset-/waardelaag + NL-teksten + "upload uitgeschakeld"-melding.

---

## Task 1: Vitest opzetten

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Vitest installeren**

Run: `npm install -D vitest`
Expected: vitest verschijnt in `devDependencies`, geen errors.

- [ ] **Step 2: test-script toevoegen**

In `package.json`, voeg in `"scripts"` toe (naast de bestaande dev/build/preview):

```json
    "test": "vitest run",
    "test:watch": "vitest"
```

- [ ] **Step 3: Verifiëren dat vitest draait**

Run: `npx vitest run`
Expected: "No test files found" (nog geen tests) — exit zonder crash.

- [ ] **Step 4: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: add vitest for demo mock tests"
```

---

## Task 2: Relatiemap

**Files:**
- Create: `src/demo/relations.js`

- [ ] **Step 1: Relatiemap schrijven**

De mock moet weten welke kolom naar welke tabel verwijst. Afgeleid uit de select-strings in `RauClient.jsx:194-195` en `RauAdmin.jsx:609,613`.

```js
// src/demo/relations.js
// Beschrijft to-one foreign keys per tabel zodat de mock geneste
// select()-strings kan resolven (zoals Supabase PostgREST embedding).
export const RELATIONS = {
  vehicles: {
    models:  { table: "models",  localKey: "model_id" },
    clients: { table: "clients", localKey: "client_id" },
  },
  models: {
    brands: { table: "brands", localKey: "brand_id" },
  },
  services: {
    vehicles: { table: "vehicles", localKey: "vehicle_id" },
  },
};
```

- [ ] **Step 2: Commit**

```bash
git add src/demo/relations.js
git commit -m "feat(demo): add FK relation map for mock joins"
```

---

## Task 3: Select-parser (TDD)

**Files:**
- Create: `src/demo/select.js`
- Test: `src/demo/__tests__/select.test.js`

- [ ] **Step 1: Failing test schrijven**

```js
// src/demo/__tests__/select.test.js
import { describe, it, expect } from "vitest";
import { parseSelect, applySelect } from "../select";

describe("parseSelect", () => {
  it("parses a flat wildcard", () => {
    expect(parseSelect("*")).toEqual({ columns: ["*"], embeds: [] });
  });

  it("parses columns plus a nested embed with a deeper embed", () => {
    const r = parseSelect("*, models(name, model_3d_path, year, brands(name))");
    expect(r.columns).toEqual(["*"]);
    expect(r.embeds).toHaveLength(1);
    expect(r.embeds[0].name).toBe("models");
    expect(r.embeds[0].parsed.columns).toEqual(["name", "model_3d_path", "year"]);
    expect(r.embeds[0].parsed.embeds[0].name).toBe("brands");
    expect(r.embeds[0].parsed.embeds[0].parsed.columns).toEqual(["name"]);
  });

  it("parses two sibling embeds", () => {
    const r = parseSelect("*, vehicles(plate, models(name, brands(name)))");
    expect(r.embeds[0].name).toBe("vehicles");
    expect(r.embeds[0].parsed.embeds[0].name).toBe("models");
  });
});

describe("applySelect", () => {
  const tables = {
    brands: [{ id: "b1", name: "Porsche" }],
    models: [{ id: "m1", brand_id: "b1", name: "911 GT3 RS", year: 2023, model_3d_path: null }],
    vehicles: [{ id: "v1", model_id: "m1", client_id: "c1", plate: "1-AAA-111" }],
  };
  const getTable = (t) => tables[t];

  it("embeds a to-one relation with nested embed", () => {
    const parsed = parseSelect("*, models(name, year, brands(name))");
    const row = applySelect(tables.vehicles[0], parsed, "vehicles", getTable);
    expect(row.plate).toBe("1-AAA-111");
    expect(row.models).toEqual({ name: "911 GT3 RS", year: 2023, brands: { name: "Porsche" } });
  });
});
```

- [ ] **Step 2: Run test, verify it fails**

Run: `npx vitest run src/demo/__tests__/select.test.js`
Expected: FAIL — "Failed to resolve import ../select" / functions not defined.

- [ ] **Step 3: Implementeer `select.js`**

```js
// src/demo/select.js
import { RELATIONS } from "./relations";

// Split op komma's op het hoogste niveau (haakjes negeren).
function splitTopLevel(str) {
  const parts = [];
  let depth = 0, cur = "";
  for (const ch of str) {
    if (ch === "(") depth++;
    if (ch === ")") depth--;
    if (ch === "," && depth === 0) { parts.push(cur); cur = ""; }
    else cur += ch;
  }
  if (cur.trim()) parts.push(cur);
  return parts.map((p) => p.trim()).filter(Boolean);
}

// "models(name, brands(name))" -> { name:"models", inner:"name, brands(name)" }
function parseEmbed(token) {
  const open = token.indexOf("(");
  const name = token.slice(0, open).trim();
  const inner = token.slice(open + 1, token.lastIndexOf(")"));
  return { name, inner };
}

export function parseSelect(str) {
  const columns = [];
  const embeds = [];
  for (const token of splitTopLevel(str)) {
    if (token.includes("(")) {
      const { name, inner } = parseEmbed(token);
      embeds.push({ name, parsed: parseSelect(inner) });
    } else {
      columns.push(token);
    }
  }
  return { columns, embeds };
}

// Past een geparste select toe op één rij.
export function applySelect(row, parsed, table, getTable) {
  if (!row) return row;
  let out;
  if (parsed.columns.includes("*") || parsed.columns.length === 0) {
    out = { ...row };
  } else {
    out = {};
    for (const c of parsed.columns) out[c] = row[c];
  }
  for (const embed of parsed.embeds) {
    const rel = RELATIONS[table]?.[embed.name];
    if (!rel) { out[embed.name] = null; continue; }
    const related = getTable(rel.table).find((r) => r.id === row[rel.localKey]);
    out[embed.name] = related ? applySelect(related, embed.parsed, rel.table, getTable) : null;
  }
  return out;
}
```

- [ ] **Step 4: Run tests, verify pass**

Run: `npx vitest run src/demo/__tests__/select.test.js`
Expected: PASS (3 + 1 tests).

- [ ] **Step 5: Commit**

```bash
git add src/demo/select.js src/demo/__tests__/select.test.js
git commit -m "feat(demo): add select-string parser with nested embeds"
```

---

## Task 4: Store met localStorage

**Files:**
- Create: `src/demo/store.js`
- (seed wordt in Task 6 ingevuld; hier een tijdelijke lege seed-import die Task 6 vervangt)

- [ ] **Step 1: Tijdelijke seed-stub maken zodat store importeert**

Maak `src/demo/seed.js` met een minimale stub (wordt in Task 6 vervangen door echte data):

```js
// src/demo/seed.js — TIJDELIJKE STUB, vervangen in Task 6
export const seedData = {
  brands: [], models: [], vehicles: [], clients: [],
  profiles: [], team: [], services: [], invoices: [], messages: [],
};
```

- [ ] **Step 2: Store implementeren**

```js
// src/demo/store.js
import { seedData } from "./seed";

const STORAGE_KEY = "rau-demo-db";

function clone(obj) { return JSON.parse(JSON.stringify(obj)); }

function load() {
  try {
    const raw = localStorage.getItem(STORAGE_KEY);
    if (raw) return JSON.parse(raw);
  } catch { /* corrupte data -> reseed */ }
  const fresh = clone(seedData);
  persist(fresh);
  return fresh;
}

function persist(db) {
  try { localStorage.setItem(STORAGE_KEY, JSON.stringify(db)); } catch { /* quota */ }
}

let db = load();

export function getTable(name) { return db[name] ?? []; }
export function setTable(name, rows) { db[name] = rows; persist(db); }
export function resetDemo() { db = clone(seedData); persist(db); }
export function genId() { return "id-" + Math.random().toString(36).slice(2, 10); }
```

- [ ] **Step 3: Commit**

```bash
git add src/demo/seed.js src/demo/store.js
git commit -m "feat(demo): add localStorage-backed store with seed stub"
```

---

## Task 5: Mock-client (TDD)

**Files:**
- Create: `src/demo/mockClient.js`
- Test: `src/demo/__tests__/mockClient.test.js`

- [ ] **Step 1: Failing test schrijven**

De test gebruikt de echte select-strings uit de app. We injecteren een vaste dataset via `setTable`.

```js
// src/demo/__tests__/mockClient.test.js
import { describe, it, expect, beforeEach, vi } from "vitest";

// localStorage shim voor node
beforeEach(() => {
  const mem = {};
  vi.stubGlobal("localStorage", {
    getItem: (k) => (k in mem ? mem[k] : null),
    setItem: (k, v) => { mem[k] = String(v); },
    removeItem: (k) => { delete mem[k]; },
  });
});

async function freshClient() {
  vi.resetModules();
  const store = await import("../store");
  store.setTable("brands", [{ id: "b1", name: "Porsche" }]);
  store.setTable("models", [{ id: "m1", brand_id: "b1", name: "911 GT3 RS", year: 2023, model_3d_path: null }]);
  store.setTable("clients", [{ id: "c1", name: "Maarten Demo", company: "Privé" }]);
  store.setTable("vehicles", [
    { id: "v1", client_id: "c1", model_id: "m1", plate: "1-AAA-111", status: "garaged", value: 320000, created_at: "2025-01-01" },
  ]);
  store.setTable("services", []);
  const { createMockClient } = await import("../mockClient");
  return createMockClient();
}

describe("mock client", () => {
  it("select with nested embed + eq + order", async () => {
    const supabase = await freshClient();
    const { data, error } = await supabase
      .from("vehicles")
      .select("*, models(name, model_3d_path, year, brands(name))")
      .eq("client_id", "c1")
      .order("created_at");
    expect(error).toBeNull();
    expect(data).toHaveLength(1);
    expect(data[0].plate).toBe("1-AAA-111");
    expect(data[0].models.brands.name).toBe("Porsche");
  });

  it("single() returns one row, not array", async () => {
    const supabase = await freshClient();
    const { data } = await supabase.from("clients").select("*").eq("id", "c1").single();
    expect(data.name).toBe("Maarten Demo");
  });

  it("insert appends a row with generated id", async () => {
    const supabase = await freshClient();
    const { error } = await supabase.from("services").insert({ client_id: "c1", type: "Test" });
    expect(error).toBeNull();
    const { data } = await supabase.from("services").select("*");
    expect(data).toHaveLength(1);
    expect(data[0].id).toBeTruthy();
  });

  it("update with eq mutates matching rows", async () => {
    const supabase = await freshClient();
    await supabase.from("vehicles").update({ status: "in-service" }).eq("id", "v1");
    const { data } = await supabase.from("vehicles").select("*").eq("id", "v1").single();
    expect(data.status).toBe("in-service");
  });

  it("storage upload is disabled but does not throw", async () => {
    const supabase = await freshClient();
    const { error } = await supabase.storage.from("3d-models").upload("x.png", {});
    expect(error).toBeTruthy();
  });

  it("auth.onAuthStateChange fires a demo session", async () => {
    const supabase = await freshClient();
    const seen = [];
    supabase.auth.onAuthStateChange((_e, session) => seen.push(session));
    await new Promise((r) => setTimeout(r, 0));
    expect(seen[0]?.user?.id).toBeTruthy();
  });
});
```

- [ ] **Step 2: Run test, verify it fails**

Run: `npx vitest run src/demo/__tests__/mockClient.test.js`
Expected: FAIL — "Failed to resolve import ../mockClient".

- [ ] **Step 3: Implementeer `mockClient.js`**

```js
// src/demo/mockClient.js
import { getTable, setTable, genId } from "./store";
import { parseSelect, applySelect } from "./select";

const DEMO_USER = { id: "demo-user", email: "demo@rau.be" };
const DEMO_SESSION = { user: DEMO_USER };

// Query-builder: thenable zodat `await supabase.from(...).select()...` werkt.
function makeQuery(table) {
  const state = {
    action: "select",
    selectStr: "*",
    filters: [],          // {col, val}
    orderBy: null,        // {col, ascending}
    single: false,
    payload: null,        // insert/update data
  };

  function rows() {
    let data = getTable(table);
    for (const f of state.filters) data = data.filter((r) => r[f.col] === f.val);
    return data;
  }

  function run() {
    try {
      if (state.action === "insert") {
        const items = (Array.isArray(state.payload) ? state.payload : [state.payload])
          .map((x) => ({ id: genId(), created_at: new Date().toISOString(), ...x }));
        setTable(table, [...getTable(table), ...items]);
        return { data: items, error: null };
      }
      if (state.action === "update") {
        const all = getTable(table);
        const updated = all.map((r) =>
          state.filters.every((f) => r[f.col] === f.val) ? { ...r, ...state.payload } : r
        );
        setTable(table, updated);
        return { data: null, error: null };
      }
      if (state.action === "delete") {
        const all = getTable(table);
        const kept = all.filter((r) => !state.filters.every((f) => r[f.col] === f.val));
        setTable(table, kept);
        return { data: null, error: null };
      }
      // select
      let data = rows();
      if (state.orderBy) {
        const { col, ascending } = state.orderBy;
        data = [...data].sort((a, b) => {
          if (a[col] === b[col]) return 0;
          const lt = a[col] < b[col] ? -1 : 1;
          return ascending ? lt : -lt;
        });
      }
      const parsed = parseSelect(state.selectStr);
      data = data.map((r) => applySelect(r, parsed, table, getTable));
      if (state.single) return { data: data[0] ?? null, error: null };
      return { data, error: null };
    } catch (err) {
      return { data: null, error: { message: String(err) } };
    }
  }

  const builder = {
    select(str = "*") { state.action = state.action === "select" ? "select" : state.action; state.selectStr = str; return builder; },
    insert(payload) { state.action = "insert"; state.payload = payload; return builder; },
    update(payload) { state.action = "update"; state.payload = payload; return builder; },
    delete() { state.action = "delete"; return builder; },
    eq(col, val) { state.filters.push({ col, val }); return builder; },
    order(col, opts = {}) { state.orderBy = { col, ascending: opts.ascending !== false }; return builder; },
    single() { state.single = true; return builder; },
    then(resolve, reject) { return Promise.resolve(run()).then(resolve, reject); },
  };
  return builder;
}

export function createMockClient() {
  return {
    from: (table) => makeQuery(table),
    auth: {
      onAuthStateChange(cb) {
        setTimeout(() => cb("SIGNED_IN", DEMO_SESSION), 0);
        return { data: { subscription: { unsubscribe() {} } } };
      },
      async signInWithPassword() { return { data: { session: DEMO_SESSION, user: DEMO_USER }, error: null }; },
      async signOut() { return { error: null }; },
      async getSession() { return { data: { session: DEMO_SESSION }, error: null }; },
    },
    storage: {
      from() {
        return {
          async upload() { return { data: null, error: { message: "Uploads zijn niet beschikbaar in de demo" } }; },
          getPublicUrl(path) { return { data: { publicUrl: path || "" } }; },
        };
      },
    },
  };
}
```

> Let op: `insert` injecteert `created_at`/`id` vóór de payload-spread zodat een meegegeven waarde wint. Dit matcht het bestaande gebruik (`messages.insert` zonder id/created_at).

- [ ] **Step 4: Run tests, verify pass**

Run: `npx vitest run src/demo/__tests__/mockClient.test.js`
Expected: PASS (6 tests).

- [ ] **Step 5: Commit**

```bash
git add src/demo/mockClient.js src/demo/__tests__/mockClient.test.js
git commit -m "feat(demo): add mock supabase client (query/auth/storage)"
```

---

## Task 6: Seed-data voor Maarten

**Files:**
- Modify: `src/demo/seed.js` (vervang de stub)

> Vehicle-kolommen (afgeleid uit de app): `id, client_id, model_id, plate, color, mileage (string), next_service, value, status, display_mode, image_path, created_at` + nieuwe asset-velden `purchase_value, current_value, value_history, condition_score, documents`. `models`: `id, brand_id, name, year, model_3d_path`. `brands`: `id, name`. Status-waarden die de UI kent: `garaged, in-service, pickup-scheduled` (vehicles); `scheduled, in-progress, completed, cancelled` (services); `draft, pending, paid, overdue` (invoices).

- [ ] **Step 1: Seed schrijven**

Vervang de volledige inhoud van `src/demo/seed.js`:

```js
// src/demo/seed.js — RAÚ demodata, gepersonaliseerd voor Maarten
export const seedData = {
  brands: [
    { id: "b-por", name: "Porsche" },
    { id: "b-amg", name: "Mercedes-AMG" },
    { id: "b-lr",  name: "Land Rover" },
    { id: "b-rr",  name: "Range Rover" },
    { id: "b-bug", name: "Bugatti" },
    { id: "b-ben", name: "Bentley" },
  ],
  models: [
    { id: "m-gt3rs",   brand_id: "b-por", name: "992 911 GT3 RS — Manthey Kit", year: 2023, model_3d_path: null },
    { id: "m-amggt",   brand_id: "b-amg", name: "AMG GT Roadster",               year: 2024, model_3d_path: null },
    { id: "m-rr",      brand_id: "b-rr",  name: "Autobiography LWB",             year: 2024, model_3d_path: null },
    { id: "m-defocta", brand_id: "b-lr",  name: "Defender OCTA V8",              year: 2024, model_3d_path: null },
    { id: "m-chiron",  brand_id: "b-bug", name: "Chiron Super Sport",            year: 2022, model_3d_path: null },
    { id: "m-conti",   brand_id: "b-ben", name: "Continental GT Speed",          year: 2023, model_3d_path: null },
  ],
  clients: [
    { id: "c-maarten", name: "Maarten", email: "maarten@privé.be", phone: "+32 470 00 00 00",
      company: "Privécollectie", tier: "Collection", since: "2026-06-01", monthly_fee: 2950,
      status: "active", avatar: "M", total_spent: 0 },
    // Enkele fictieve klanten zodat het admingedeelte gevuld oogt:
    { id: "c-vdb", name: "Alexander Van den Berg", email: "alexander@vdb-holding.be", phone: "+32 475 12 34 56",
      company: "VDB Holding", tier: "Signature", since: "2026-03-15", monthly_fee: 1500, status: "active", avatar: "AV", total_spent: 12400 },
    { id: "c-dub", name: "Marie-Claire Dubois", email: "mc@dubois-invest.be", phone: "+32 476 98 76 54",
      company: "Dubois Investments", tier: "Essential", since: "2026-04-10", monthly_fee: 750, status: "active", avatar: "MD", total_spent: 3600 },
  ],
  profiles: [
    { id: "demo-user", role: "admin", client_id: null },
  ],
  vehicles: [
    // Maartens collectie — Bugatti = kroonjuweel (eerst, voor de wow op het dashboard)
    veh("v-chiron", "c-maarten", "m-chiron", "1-BUG-001", "Bleu Royal Carbon", "1.240 km", "2026-09-01",
        3650000, 3950000, "garaged", [{ d: "2022-01", v: 3650000 }, { d: "2024-01", v: 3800000 }, { d: "2026-06", v: 3950000 }], 99),
    veh("v-gt3rs", "c-maarten", "m-gt3rs", "1-RS-911", "Arctic Grey", "3.480 km", "2026-07-15",
        290000, 335000, "garaged", [{ d: "2023-06", v: 290000 }, { d: "2024-06", v: 312000 }, { d: "2026-06", v: 335000 }], 98),
    veh("v-conti", "c-maarten", "m-conti", "1-BEN-007", "Beluga Black", "9.120 km", "2026-08-10",
        298000, 282000, "garaged", [{ d: "2023-03", v: 298000 }, { d: "2026-06", v: 282000 }], 95),
    veh("v-octa", "c-maarten", "m-defocta", "1-OCT-008", "Carpathian Grey", "5.600 km", "2026-07-28",
        185000, 190000, "in-service", [{ d: "2024-02", v: 185000 }, { d: "2026-06", v: 190000 }], 96),
    veh("v-amggt", "c-maarten", "m-amggt", "1-AMG-009", "Magno Grey", "7.300 km", "2026-10-05",
        178000, 172000, "garaged", [{ d: "2024-04", v: 178000 }, { d: "2026-06", v: 172000 }], 94),
    veh("v-rr", "c-maarten", "m-rr", "1-RR-010", "Santorini Black", "11.450 km", "2026-09-20",
        162000, 154000, "pickup-scheduled", [{ d: "2024-01", v: 162000 }, { d: "2026-06", v: 154000 }], 93),
  ],
  services: [
    { id: "s1", vehicle_id: "v-octa", client_id: "c-maarten", type: "Bandenwissel",
      description: "Wissel naar zomerset — Michelin Pilot Sport", status: "in-progress",
      date: "2026-06-25", technician: "Kevin Martens", priority: "high", estimated_cost: 3200, created_at: "2026-06-20" },
    { id: "s2", vehicle_id: "v-rr", client_id: "c-maarten", type: "Ophaling & detailing",
      description: "Ophaling bij klant, volledige detailing, ceramic touch-up", status: "scheduled",
      date: "2026-06-28", technician: "Yannick De Wolf", priority: "normal", estimated_cost: 1450, created_at: "2026-06-19" },
    { id: "s3", vehicle_id: "v-chiron", client_id: "c-maarten", type: "Jaarlijkse keuring",
      description: "Bugatti certified inspectie — meerdaags proces", status: "scheduled",
      date: "2026-09-01", technician: "Kevin Martens", priority: "high", estimated_cost: 12500, created_at: "2026-06-15" },
    { id: "s4", vehicle_id: "v-gt3rs", client_id: "c-maarten", type: "Stallingsvoorbereiding",
      description: "Battery tender, bandenspanning, hoes geplaatst", status: "completed",
      date: "2026-06-10", technician: "Yannick De Wolf", priority: "low", estimated_cost: 350, created_at: "2026-06-08" },
  ],
  invoices: [
    { id: "i1", client_id: "c-maarten", amount: 2950, type: "Abonnement", description: null, period: "Juni 2026", status: "paid", date: "2026-06-01", created_at: "2026-06-01" },
    { id: "i2", client_id: "c-maarten", amount: 3200, type: "Service", description: "Defender OCTA — Bandenwissel", period: null, status: "pending", date: "2026-06-25", created_at: "2026-06-25" },
  ],
  messages: [
    { id: "msg1", client_id: "c-maarten", subject: "Welkom bij RAÚ", body: "Maarten, uw collectie is volledig opgenomen in uw portaal. Bekijk gerust de waarde-evolutie.", direction: "outgoing", read: false, created_at: "2026-06-20T09:00:00Z" },
    { id: "msg2", client_id: "c-maarten", subject: "Chiron — keuring gepland", body: "De jaarlijkse Bugatti-keuring staat gepland op 1 september. Wij coördineren alles.", direction: "outgoing", read: false, created_at: "2026-06-21T14:00:00Z" },
  ],
  team: [
    { id: "t1", name: "Kevin Martens", role: "Hoofdtechnicus", speciality: "Motor & aandrijflijn", avatar: "KM", status: "busy", active_tasks: 2 },
    { id: "t2", name: "Yannick De Wolf", role: "Detailing-specialist", speciality: "Paint correction & ceramic", avatar: "YD", status: "busy", active_tasks: 1 },
    { id: "t3", name: "Jonas Vermeersch", role: "Technicus", speciality: "Diagnose & elektronica", avatar: "JV", status: "available", active_tasks: 0 },
    { id: "t4", name: "Lisa Claes", role: "Klantenrelaties", speciality: "Planning & communicatie", avatar: "LC", status: "busy", active_tasks: 3 },
  ],
};

// Helper voor vehicles incl. nieuwe asset-velden.
function veh(id, client_id, model_id, plate, color, mileage, next_service, purchase_value, current_value, status, value_history, condition_score) {
  return {
    id, client_id, model_id, plate, color, mileage, next_service,
    value: current_value, purchase_value, current_value, value_history, condition_score,
    status, display_mode: "image", image_path: "",
    documents: [
      { type: "Verzekeringspolis", status: "Actief", date: "2026-01-01" },
      { type: "Technische keuring", status: "Geldig", date: "2026-03-01" },
      { type: "Aankoopfactuur", status: "Gearchiveerd", date: "—" },
    ],
    created_at: "2026-06-01",
  };
}
```

> `value` blijft gevuld (bestaande UI op `RauClient.jsx:302` gebruikt `v.value`). `current_value`/`value_history` voeden de nieuwe asset-laag (Task 9).
> `function veh(...)` staat ná het object — JS hoist functiedeclaraties, dus dit werkt.

- [ ] **Step 2: Tests blijven groen**

Run: `npx vitest run`
Expected: PASS (alle tests; seed-vorm breekt niets).

- [ ] **Step 3: Commit**

```bash
git add src/demo/seed.js
git commit -m "feat(demo): seed Maarten's collection + NL demo data"
```

---

## Task 7: Mock activeren in supabase.js

**Files:**
- Create: `src/supabase.real.js`
- Modify: `src/supabase.js`

- [ ] **Step 1: Echte client bewaren**

Kopieer de huidige inhoud van `src/supabase.js` naar `src/supabase.real.js` (ongewijzigd — de echte `createClient`-versie).

- [ ] **Step 2: supabase.js naar mock laten wijzen**

Vervang de volledige inhoud van `src/supabase.js`:

```js
// DEMO-MODUS: geen echte database. De mock-client leeft op localStorage.
// Terug naar echte Supabase? Herstel de inhoud van supabase.real.js hier.
import { createMockClient } from "./demo/mockClient";

export const supabase = createMockClient();
```

- [ ] **Step 3: Dev-server draait zonder DB-fouten**

Run: `npm run dev` (achtergrond) en open de preview.
Expected: app laadt; geen netwerkcalls naar `supabase.co`; geen console-errors over fetch/auth.

- [ ] **Step 4: Commit**

```bash
git add src/supabase.js src/supabase.real.js
git commit -m "feat(demo): route supabase.js to mock client"
```

---

## Task 8: Login overslaan + rol-schakelaar

**Files:**
- Modify: `src/main.jsx`

- [ ] **Step 1: main.jsx herschrijven**

Vervang de volledige inhoud van `src/main.jsx`:

```jsx
import { useState, useEffect } from "react";
import ReactDOM from "react-dom/client";
import { supabase } from "./supabase";
import AdminDashboard from "./RauAdmin";
import ClientPortal from "./RauClient";
import { resetDemo } from "./demo/store";

const ROLE_KEY = "rau-demo-role";

function DemoBar({ role, setRole }) {
  const btn = (active) => ({
    padding: "5px 12px", fontSize: 10, letterSpacing: "0.12em", fontFamily: "'JetBrains Mono', monospace",
    border: "1px solid " + (active ? "#8a9a6e" : "rgba(255,255,255,0.12)"),
    background: active ? "rgba(138,154,110,0.12)" : "transparent",
    color: active ? "#a0b27e" : "#6a6a64", borderRadius: 6, cursor: "pointer",
  });
  return (
    <div style={{ position: "fixed", top: 10, right: 12, zIndex: 9999, display: "flex", gap: 6, alignItems: "center",
      background: "rgba(8,8,8,0.85)", padding: "6px 8px", borderRadius: 10, backdropFilter: "blur(8px)", border: "1px solid rgba(255,255,255,0.06)" }}>
      <span style={{ fontSize: 8, letterSpacing: "0.2em", color: "#3e3e3a", fontFamily: "'JetBrains Mono', monospace", marginRight: 2 }}>DEMO</span>
      <button style={btn(role === "admin")} onClick={() => setRole("admin")}>ADMIN</button>
      <button style={btn(role === "client")} onClick={() => setRole("client")}>MAARTEN</button>
      <button style={btn(false)} onClick={() => { if (confirm("Demodata terugzetten?")) { resetDemo(); location.reload(); } }}>RESET</button>
    </div>
  );
}

function App() {
  const [role, setRole] = useState(() => localStorage.getItem(ROLE_KEY) || "admin");
  useEffect(() => { localStorage.setItem(ROLE_KEY, role); }, [role]);

  const demoUser = { id: "demo-user", email: "demo@rau.be" };
  const signOut = () => setRole(role === "admin" ? "client" : "admin");

  return (
    <>
      <DemoBar role={role} setRole={setRole} />
      {role === "admin"
        ? <AdminDashboard user={demoUser} onSignOut={signOut} />
        : <ClientPortal user={demoUser} clientId="c-maarten" onSignOut={signOut} />}
    </>
  );
}

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
```

> `clientId="c-maarten"` koppelt het portaal aan Maartens seed-client. De rol-schakelaar vervangt het inlogscherm; `Login.jsx` wordt niet meer geïmporteerd.

- [ ] **Step 2: Verifiëren in browser**

Run/herlaad de preview.
Expected: rechtsboven een DEMO-balk; ADMIN toont het admindashboard, MAARTEN het klantportaal met zijn 6 wagens; RESET herlaadt met verse data.

- [ ] **Step 3: Commit**

```bash
git add src/main.jsx
git commit -m "feat(demo): skip login, add Admin/Maarten role switch + reset"
```

---

## Task 9: Asset-/waardelaag in klantportaal

**Files:**
- Modify: `src/RauClient.jsx`

> Doel: (a) totale collectiewaarde + trend op het dashboard, (b) per wagen waarde-evolutie + conditie + documenten in de "Mijn Wagens"-sectie. Binnen de bestaande donkere stijl en helpers (`fmtVal`, `C`, `mono`, `serif`).

- [ ] **Step 1: Collectiewaarde berekenen**

In `ClientPortal`, na de afgeleide waarden (rond `RauClient.jsx:222`), voeg toe:

```jsx
const totalValue = vehicles.reduce((sum, v) => sum + (v.current_value ?? v.value ?? 0), 0);
const totalPurchase = vehicles.reduce((sum, v) => sum + (v.purchase_value ?? v.value ?? 0), 0);
const valueDelta = totalValue - totalPurchase;
const valuePct = totalPurchase ? ((valueDelta / totalPurchase) * 100) : 0;
```

- [ ] **Step 2: Collectiewaarde-kaart op het dashboard tonen**

Zoek in de dashboard-render (het `nav === "dashboard"`-gedeelte, ná regel 264's sectie-blok) een logische plek bij de wagen-info en voeg een kaart toe:

```jsx
<div style={{ background: C.panel, border: `1px solid ${C.panelBorder}`, borderRadius: 14, padding: "20px 22px", marginTop: 14 }}>
  <div style={{ fontSize: 9, letterSpacing: "0.25em", color: C.textMuted, fontFamily: mono }}>TOTALE COLLECTIEWAARDE</div>
  <div style={{ fontSize: 30, fontFamily: serif, color: C.goldBright, marginTop: 6 }}>{fmtVal(totalValue)}</div>
  <div style={{ fontSize: 11, fontFamily: mono, marginTop: 4, color: valueDelta >= 0 ? C.green : C.red }}>
    {valueDelta >= 0 ? "▲" : "▼"} {fmtVal(Math.abs(valueDelta))} ({valuePct.toFixed(1).replace(".", ",")}%) sinds aankoop
  </div>
  <div style={{ fontSize: 9, color: C.textDark, marginTop: 8 }}>{vehicles.length} wagens in beheer</div>
</div>
```

- [ ] **Step 3: Waarde-evolutie + conditie + documenten per wagen**

In de "Mijn Wagens"-kaart (`nav === "wagens"`, na het `v.value`-blok op regel 302), voeg binnen dezelfde kaart toe:

```jsx
{/* Waarde-evolutie (mini-sparkline via inline staafjes) */}
{Array.isArray(v.value_history) && v.value_history.length > 1 && (() => {
  const vals = v.value_history.map(p => p.v);
  const min = Math.min(...vals), max = Math.max(...vals), span = max - min || 1;
  return (
    <div style={{ marginTop: 12 }}>
      <div style={{ fontSize: 8, letterSpacing: "0.2em", color: C.textDark, marginBottom: 6 }}>WAARDE-EVOLUTIE</div>
      <div style={{ display: "flex", alignItems: "flex-end", gap: 4, height: 36 }}>
        {v.value_history.map((p, i) => (
          <div key={i} title={`${p.d}: ${fmtVal(p.v)}`}
            style={{ flex: 1, height: `${20 + ((p.v - min) / span) * 80}%`, background: i === v.value_history.length - 1 ? C.gold : C.goldDim, borderRadius: 2 }} />
        ))}
      </div>
    </div>
  );
})()}
{/* Conditiescore */}
{v.condition_score != null && (
  <div style={{ marginTop: 12, display: "flex", justifyContent: "space-between", fontSize: 10, fontFamily: mono }}>
    <span style={{ color: C.textDark, letterSpacing: "0.2em" }}>CONDITIE</span>
    <span style={{ color: C.goldBright }}>{v.condition_score}/100</span>
  </div>
)}
{/* Documenten */}
{Array.isArray(v.documents) && (
  <div style={{ marginTop: 12, paddingTop: 12, borderTop: `1px solid rgba(255,255,255,0.06)` }}>
    <div style={{ fontSize: 8, letterSpacing: "0.2em", color: C.textDark, marginBottom: 6 }}>DOCUMENTEN</div>
    {v.documents.map((d, i) => (
      <div key={i} style={{ display: "flex", justifyContent: "space-between", fontSize: 10, color: C.text, padding: "2px 0" }}>
        <span>{d.type}</span><span style={{ color: C.textMuted, fontFamily: mono }}>{d.status}</span>
      </div>
    ))}
  </div>
)}
```

- [ ] **Step 4: Verifiëren in browser**

Herlaad de preview, ga naar MAARTEN → dashboard (collectiewaarde zichtbaar) en → Mijn Wagens (evolutie/conditie/documenten per kaart).
Expected: totale waarde ~€4,95M, Bugatti-kaart toont stijgende evolutie; geen layout-breuk.

- [ ] **Step 5: Commit**

```bash
git add src/RauClient.jsx
git commit -m "feat(demo): add collection value + per-vehicle asset layer"
```

---

## Task 10: NL-teksten + upload-melding in admin

**Files:**
- Modify: `src/RauAdmin.jsx`

- [ ] **Step 1: Engelse data-/UI-strings naar NL**

Zoek in `RauAdmin.jsx` naar resterende Engelse strings en vertaal ze (de in-code arrays op regels 35-115 worden niet meer als databron gebruikt, maar kunnen nog UI voeden — controleer en vertaal waar zichtbaar). Concreet te vertalen labels die in de UI verschijnen:

- Service-types: "Full Service" → "Volledige beurt", "Tire Change" → "Bandenwissel", "Pickup & Detailing" → "Ophaling & detailing", "Storage Prep" → "Stallingsvoorbereiding", "Annual Inspection" → "Jaarlijkse keuring".
- Team-rollen/speciality: "Lead Technician" → "Hoofdtechnicus", "Detailing Specialist" → "Detailing-specialist", "Technician" → "Technicus", "Client Relations" → "Klantenrelaties", "Engine & Drivetrain" → "Motor & aandrijflijn", "Diagnostics & Electronics" → "Diagnose & elektronica", "Scheduling & Communications" → "Planning & communicatie".

Run vooraf om de exacte plekken te vinden:
Run: `grep -nE "Full Service|Tire Change|Pickup & Detailing|Storage Prep|Annual Inspection|Lead Technician|Detailing Specialist|Client Relations|Engine & Drivetrain|Diagnostics|Scheduling" src/RauAdmin.jsx`

- [ ] **Step 2: Upload-knoppen — nette demo-melding**

Zoek de upload-handler(s) (rond `RauAdmin.jsx`, waar `supabase.storage.from(...).upload(...)` wordt aangeroepen, en `setUploading`). Zorg dat bij een upload de error van de mock netjes getoond wordt i.p.v. stil te falen, bv.:

```jsx
const { error } = await supabase.storage.from("3d-models").upload(path, file);
if (error) { setSettingsMsg("Uploads zijn niet beschikbaar in de demo."); setUploading(false); return; }
```

Run om de upload-plek te vinden:
Run: `grep -n "storage.from\|setUploading\|\.upload(" src/RauAdmin.jsx`

- [ ] **Step 3: Verifiëren in browser**

Herlaad, ga naar ADMIN → Services/Team en Instellingen.
Expected: Nederlandse teksten; bij een poging tot upload verschijnt "Uploads zijn niet beschikbaar in de demo."

- [ ] **Step 4: Commit**

```bash
git add src/RauAdmin.jsx
git commit -m "feat(demo): NL strings + disabled-upload notice in admin"
```

---

## Task 11: Pitch-spiekbriefje

**Files:**
- Create: `docs/pitch-maarten.md`

- [ ] **Step 1: Spiekbriefje schrijven**

```markdown
# Pitch-spiekbriefje — RAÚ voor Maarten

## In één zin
RAÚ is *private banking voor exclusieve wagens*: één vertrouwd aanspreekpunt dat je volledige
collectie beheert — onderhoud, stalling, transport, detailing — én de waarde ervan bewaakt.

## Verhaallijn tijdens de demo (rol-schakelaar rechtsboven)
1. **MAARTEN → Dashboard.** "Dit is jouw portaal." Toon zijn collectie + de **totale collectiewaarde (~€4,95M)** met trend. → het portfolio-gevoel.
2. **Een wagen (Chiron).** Open Mijn Wagens, toon de Bugatti: waarde-evolutie omhoog, conditie 99/100, documenten op orde. → de wagen als belegging.
3. **Services.** Toon de geplande keuring + ophaling. "Jij hoeft niets te doen — wij coördineren alles." → ontzorging.
4. **ADMIN.** Wissel naar admin. "Achter de schermen draait de machine: planning, team, facturatie." → het is een echte business.

## Kernzinnen
- "Merkdealers doen losse interventies. RAÚ is je vaste regisseur voor alles."
- "Je wagens zijn niet alleen plezier — ze zijn een portfolio. Wij bewaken die waarde."
- "Ik ken deze wereld van binnenuit. Vertrouwen en discretie zijn de kern."
- "In Vlaanderen bestaat dit nog niet op dit niveau. We zijn er vroeg bij."

## De ask
"Maarten, ik geloof hierin en jij bent precies de persoon die dit mee groot kan maken.
Zou je hierin willen meedenken — als eerste klant met je collectie, en als partner die mee opzet?"

## Belangrijk
- Demo draait lokaal op de laptop, geen internet nodig.
- Cijfers zijn indicatief — pas waarden/tarieven aan als Maarten de markt beter kent.
```

- [ ] **Step 2: Commit**

```bash
git add docs/pitch-maarten.md
git commit -m "docs: add pitch cheat-sheet for Maarten meeting"
```

---

## Task 12: Eindverificatie

- [ ] **Step 1: Alle tests groen**

Run: `npx vitest run`
Expected: PASS (select + mockClient suites).

- [ ] **Step 2: Build slaagt**

Run: `npm run build`
Expected: build zonder errors.

- [ ] **Step 3: Volledige doorloop in browser**

Run/herlaad de preview en loop de verhaallijn uit het spiekbriefje door (MAARTEN dashboard → wagens → services → berichten → ADMIN alle secties). Maak een wijziging (bv. status van een service), herlaad, controleer dat ze bewaard blijft; klik RESET en controleer dat de seed terugkomt.
Expected: geen console-errors; data persistent; reset werkt.

- [ ] **Step 4: Verificatie-bewijs delen**

Maak screenshots van Maartens dashboard (collectiewaarde) en een wagenkaart (waarde-evolutie) en deel ze.

---

## Self-Review (uitgevoerd)

- **Spec-dekking:** mock-laag (T2-T7), localStorage+reset (T4,T8), personalisatie Maarten (T6), asset-laag alle 4 (T6 data + T9 UI; documenten/conditie/waarde gedekt), NL (T6,T10), tiers (T6 client.tier + monthly_fee; tarieven in spec §7 — UI toont tier-label, prijzen leven in seed), login skip + rol-switch (T8), uploads uit (T5,T10), pitch-doc (T11). ✓
- **Tier-detailscherm:** de spec noemt tiers tonen; de demo toont de tier per klant (`tier`, `monthly_fee`) in admin/portaal. Een apart prijzen-overzichtsscherm is niet vereist voor de pitch (YAGNI) — indien gewenst los toe te voegen.
- **Placeholder-scan:** geen TBD/TODO; alle codestappen bevatten volledige code.
- **Type-consistentie:** `getTable/setTable/resetDemo/genId` consistent in store↔mockClient; `parseSelect/applySelect`-signaturen consistent in select↔mockClient; vehicle-velden (`current_value/value_history/condition_score/documents`) consistent tussen seed (T6) en UI (T9); `clientId="c-maarten"` matcht seed-client-id.
